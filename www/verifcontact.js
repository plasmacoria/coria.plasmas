function verification(){	var nom=document.form.nom.value;	var mail=document.form.mail.value;	var objet=document.form.objet.value;	var message=document.form.message.value;	/*récuperer caractères dans date du caractère 1 à 2 => (0,2) = indice, 2 non inclus*/	jour=parseInt(jour);	/*Conversion d'une chaine de caractère en entier*/		if (document.getElementById('nom').value=="")		alert("Veuillez saisir votre nom");	if (document.getElementById('objet').value=="")		alert("Veuillez saisir un objet");	if (document.getElementById('message').value=="")		alert("Veuillez saisir un message");			if (mail.indexOf("@")<0)	/*IndexOf renvoi la position du caractère que l'on recherche, ici @*/		alert("mail invalide");		/* pas besoin de {} dans if si 1 seule instruction */}